# Production is served by FastAPI from the same origin as the API.
# Leave these blank so Cloudflare HTTPS pages use same-origin HTTPS/WSS
# instead of blocked http://localhost mixed-content requests.
REACT_APP_API_URL=
REACT_APP_WS_URL=
